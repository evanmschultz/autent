// Package sqlite contains SQLite-backed repository adapters for autent.
//
// It supports both dedicated autent database files and caller-owned shared
// database handles with prefixed table names.
package sqlite
